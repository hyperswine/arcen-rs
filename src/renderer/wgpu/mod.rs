// What shaders and commands to go from vdom?

// For vertices, mostly Vec2

// Transform the ndoe hierarchy into wgpu code

use super::node::Node;

pub fn render_tree(root: &Node) {
    // walk each node and children
}
